import { AllocationPipe } from './allocation.pipe';

describe('AllocationPipe', () => {
  it('create an instance', () => {
    const pipe = new AllocationPipe();
    expect(pipe).toBeTruthy();
  });
});
